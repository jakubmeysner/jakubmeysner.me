const colors = require('tailwindcss/colors')

module.exports = {
  theme: {
    fontFamily: {
      sans: ['InterVariable', 'system-ui', 'sans-serif']
    },
    colors: {
      gray: colors.trueGray
    }
  }
}
